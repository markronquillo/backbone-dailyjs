suite('App', function() {
    test('bTask should be present', function() {
        assert.ok(window.bTask);
    });


    test('gapi should be present', function() {
        assert.ok(window.gapi);
    });
});

